import styles from './ChatInterface.module.css';

export const ChatHeader = () => {
    return (
        <header className={styles.header}>
            <h1 className={styles.title}>Growth With Flow Agent</h1>
        </header>
    );
};
